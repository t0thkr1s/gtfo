{
  "description": "The output is a hex dump.",
  "functions": {
    "file-read": [
      {
        "code": "hexdump -C \"[file]\"\n"
      }
    ],
    "suid": [
      {
        "code": "./hexdump -C \"[file]\"\n"
      }
    ],
    "sudo": [
      {
        "code": "sudo hexdump -C \"[file]\"\n"
      }
    ]
  }
}